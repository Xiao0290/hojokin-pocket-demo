Bottom navigation with the four app sections and built-in line icons.

```jsx
<TabBar active={tab} onChange={setTab} />
```

- Default ids: `diag` (診断) · `apply` (申請) · `msg` (メッセージ) · `mypage` (マイページ)
- Active tab text + icon turn brand blue
